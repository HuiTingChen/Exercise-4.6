package AbstractSuperClass;

interface TotalPrice {
	
	double totalPrice(double p, double q); // method with no body
	
	double totalPrice(double p, double q, double dis); // method with no body
}
